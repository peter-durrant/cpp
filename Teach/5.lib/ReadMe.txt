Demonstrate creating and using a static library called functions.a

The static library consists of the code in functions.cpp and uses the interface definition in functions.h
The main executable includes the interface (functions.h) to correctly call the functions and links to the static library to provide the function logic.
